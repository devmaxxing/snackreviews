{
  "routes": [
    {
      "method": "GET",
      "path": "/productlines",
      "handler": "Productline.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/productlines/count",
      "handler": "Productline.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/productlines/:_id",
      "handler": "Productline.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/productlines",
      "handler": "Productline.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/productlines/:_id",
      "handler": "Productline.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/productlines/:_id",
      "handler": "Productline.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
